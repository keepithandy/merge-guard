# v1.1 adoption and diagnostics preparation evidence

Status: **PREPARED FOR REVIEW — NOT A V1.1 EXIT-GATE DECISION**

## Candidate

- Source commit: `c9fc6be3ec0c0543cea6e722f0c67a2ca5b458f3`
- Scope: supported journey matrix, deterministic doctor diagnostics, consumer conformance fixtures, and privacy-preserving feedback/support loop
- Local environment: Windows, Node `v24.16.0`

## Implemented evidence

- `merge-guard --doctor` provides deterministic text diagnostics; `--doctor --json` provides schema-versioned structured diagnostics.
- Doctor checks the runtime, package/SBOM identity, local configuration, selected policy/plugin manifests, repository markers, Action contract, and optional caller Action inputs without executing project commands or accessing the network.
- `docs/adoption-and-diagnostics.md` defines source, local package, npm package, Action, dashboard, policy, and plugin journeys with recovery paths.
- `test/fixtures/consumer-conformance/` covers standalone Node, npm workspace, Python, mixed, forked pull request, and restricted-permission Action inputs without secrets or execution.
- `.github/ISSUE_TEMPLATE/` contains opt-in adoption and doctor feedback forms; `docs/support-process.md` records the no-telemetry boundary.

## Local validation

`npm run release:check` completed with **114/114 passing**. It includes the new doctor and consumer-fixture contracts plus CLI, public-contract, support, installation, security, performance, artifact, distribution, and release-readiness checks. Validation did not publish a package, create a release, create or move a tag, change an Action reference, sign an artifact, or contact an external service.

## Remaining validation gaps

- GitHub Actions must run the final candidate through Node 18, 20, 22, and 24 on Ubuntu and Windows after a pull request is opened.
- The public npm package journey remains unavailable because v1.0 npm publication is separately unapproved. The local archive path is covered; this is not a claim that the public registry path passed.
- v1.0 remains prepared but unsigned, unpublished, and unverified. This v1.1 preparation neither changes nor authorizes any external v1.0 release action.

## Milestone status

The v1.1 exit gate is not marked complete. A final reviewer must evaluate the pull-request matrix and decide how the formal v1.0 release decision interacts with milestone sequencing. Passing local validation is evidence, not approval or publication.
