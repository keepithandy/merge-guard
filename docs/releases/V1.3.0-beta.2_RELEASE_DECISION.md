# v1.3.0-beta.2 release preparation

The owner requested merging the version-command fix, preparing a new beta, publishing its GitHub prerelease, and verifying the downloaded archive. Target version: merge-guard@1.3.0-beta.2.

Before publication, record the exact merged source SHA, run release readiness, verify two identical package builds and checksums, and confirm the supported Node/OS CI matrix passes on the candidate. Staging records the source and build evidence in PROVENANCE.json.

Authorized scope: the new beta tag and experimental GitHub prerelease with its package, checksums, SBOM, provenance, and preparation records. npm publication, signing, and stable Action-reference changes are outside this release.

Preparation does not assert successful publication. Verify the uploaded tag and archive, install the downloaded package in isolation, and check its version and help before reporting completion. On a failed gate, stop publication and retain evidence; do not overwrite an existing release archive.
