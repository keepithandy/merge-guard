# Merge Guard v1.3.0-beta.1 release decision packet

Status: **BETA SOURCE IDENTITY RECORDED — PREPARATION, APPROVAL, PUBLICATION, AND VERIFICATION PENDING**

This is a non-publishing decision template for the current `1.3.0-beta.1` beta source. Passing local or CI checks does not authorize signing, tag creation, npm publication, a GitHub release, a stable Action-reference change, or a Marketplace listing.

## Independent release states

| State | Current value | Meaning |
| --- | --- | --- |
| Beta source identity recorded | Yes | Package, runtime, SBOM, reports, and current-version documentation identify `1.3.0-beta.1`. |
| Prepared | No staged beta packet yet | An immutable commit, reproducible package builds, subject checksums, and toolchain evidence are recorded. |
| Approved | No | A named owner has not authorized an external mutation. |
| Published | No | No distribution channel has changed. |
| Verified | No | No post-publication installation or Action check exists. |

## Required preparation evidence

- full reviewed source SHA and clean-checkout status;
- `npm run release:check`, including beta identity and evaluation-design contracts;
- two byte-identical clean package builds and a SHA-256 subject manifest;
- package/SBOM identity for `merge-guard@1.3.0-beta.1`;
- exact Node 18/20/22/24 × Ubuntu/Windows workflow evidence;
- known limitations, evaluation plan, support handoff, abort, rollback, and revocation procedures.

Stage only an immutable reviewed commit with:

```bash
npm run release:stage -- release/v1.3.0-beta.1
```

That command does not sign, tag, publish, move an Action reference, create credentials, or infer approval.

## Owner decision record

- Decision: **pending**
- Release owner: **not recorded**
- Decision time (UTC): **not recorded**
- Reviewed source commit: **not recorded**
- Staged artifact and checksum manifest: **not recorded**
- Authorized external mutations: **none**

Each signing, tag, npm publication, GitHub release, Action-reference, and Marketplace mutation requires a separate explicit owner decision.
