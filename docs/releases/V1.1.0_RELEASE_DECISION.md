# Merge Guard v1.1.0 release decision packet

Status: **SOURCE VERSION RECORDED — PREPARATION, APPROVAL, PUBLICATION, AND VERIFICATION PENDING**

This is a non-publishing decision template for the current `1.1.0` source version. Passing local or CI checks does not authorize signing, tag creation, npm publication, a GitHub release, a stable Action-reference change, or a Marketplace listing.

## Independent release states

| State | Current value | Meaning |
| --- | --- | --- |
| Source version recorded | Yes | Current package, runtime, SBOM, reports, and current-version documentation identify `1.1.0`. |
| Prepared | No staged v1.1.0 packet yet | An immutable commit, reproducible package builds, full subject checksums, and toolchain evidence are recorded. |
| Approved | No | A named owner has not authorized any external mutation. |
| Published | No | No distribution channel has been changed. |
| Verified | No | No post-publication check exists. |

## Required preparation evidence

- full reviewed source SHA and clean-checkout status;
- `npm run release:check` result, including current version contracts;
- two byte-identical clean package builds and a SHA-256 subject manifest;
- package/SBOM identity for `merge-guard@1.1.0`;
- exact successful Node 18/20/22/24 × Ubuntu/Windows workflow evidence;
- known limitations, support handoff, abort, rollback, and revocation plan.

Stage only an immutable reviewed commit with:

```bash
npm run release:stage -- release/v1.1.0
```

That command does not sign, tag, publish, move an Action reference, create credentials, or infer approval.

## Owner decision record

- Decision: **pending**
- Release owner: **not recorded**
- Decision time (UTC): **not recorded**
- Reviewed source commit: **not recorded**
- Staged artifact and checksum manifest: **not recorded**
- Authorized external mutations: **none**

Each signing, tag, npm publication, GitHub release, Action-reference, and Marketplace mutation requires a separate explicit owner decision. Use the documented release-artifact, migration, support, and provenance procedures for abort, rollback, revocation, and handoff.
